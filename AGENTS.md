# 全局开发配置

> 借鉴自 https://github.com/bd-dxg/my-pi (MIT)，按本机环境适配。

## 语言和环境

- **语言**: 所有输出（包括思考过程、回复、代码注释和 commit 信息）一律使用简体中文
- **操作系统**: Windows 11（150% DPI 缩放） | **AI 终端**: Git Bash (MSYS2) | **用户终端**: PowerShell / Windows Terminal
- **已安装**: Node.js v22（nvm 管理）、Python、Rust（GNU 工具链）、GitHub CLI（`gh`）
- **编码注意**: PowerShell 5.1 读 UTF-8 文件会按 GBK 解析 → 必须显式 `[System.IO.File]::ReadAllText(..., UTF8)`；`.ps1` 脚本避免非 ASCII 注释

## 权限

- 拥有读取任意文件的权限，无需询问确认
- 危险命令/写受保护路径会经 permission-gate 确认；**先解释为什么需要，再等用户决定**

## 编码原则（核心哲学）

### 1. 先思考，再编码

- 明确说明假设；有多种解读时，列出选项，不要悄悄选一个
- 遇到更简单的方案，主动说出来；真正不清楚时，**停下来问**（可用 question 工具），而不是猜

### 2. 简洁优先

- 只实现被要求的功能，不写投机性代码
- 单次使用的代码不做抽象；不要"未来可能用到"的灵活性

### 3. 外科手术式修改

- 只改必须改的地方；不"顺手优化"无关代码
- 保持现有代码风格，即使你会用不同写法
- 你的改动产生的孤儿代码（无用 import/变量）→ 删掉；原有死代码 → 仅提及，不删除

### 4. 目标驱动执行

将任务转化为可验证的目标：

- "修复 bug" → "写一个能复现它的测试，然后让它通过"
- "重构 X" → "确保重构前后测试都通过"

多步骤任务先列计划：

```text
1. [步骤] → 验证: [检查点]
2. [步骤] → 验证: [检查点]
```

## 命令执行策略

### AI 自动执行（✅ 允许）

- 只读命令：`ls` `cat` `grep` `find` `git status/diff/log` `node -e`（查询类）等
- 项目内文件编辑（write/edit 工具）
- 安装依赖：`npm install`（项目内）

### 需确认（⚠️ 询问用户）

- 删除/覆盖文件、修改配置（settings.json 等）
- 系统级操作：关机、重启、磁盘、sudo
- 未在允许列表中的新命令

## 项目专属约定

- **2-pi-r 扩展改动**：改完必须验证（语法 + 行为测试），commit 信息用英文、描述改动点
- **pi 扩展调试**：扩展事件日志写在 `~/.pi/agent/loop-debug.log`（loop）等文件，排查先看日志
- **金价/行情**：新浪接口 `hq.sinajs.cn`（需 Referer: https://finance.sina.com.cn，GBK 编码用 TextDecoder("gbk")）
